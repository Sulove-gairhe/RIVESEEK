/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import { RIVESEEK_GOAL_VAULT_PROGRAM_ADDRESS } from "../programs";
import { getAccountMetaFactory, type ResolvedAccount } from "../shared";

export const CANCEL_GOAL_DISCRIMINATOR = new Uint8Array([
  57, 96, 8, 4, 24, 186, 78, 187,
]);

export function getCancelGoalDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(CANCEL_GOAL_DISCRIMINATOR);
}

export type CancelGoalInstruction<
  TProgram extends string = typeof RIVESEEK_GOAL_VAULT_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountGoalAccount extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? ReadonlySignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountGoalAccount extends string
        ? WritableAccount<TAccountGoalAccount>
        : TAccountGoalAccount,
      ...TRemainingAccounts,
    ]
  >;

export type CancelGoalInstructionData = { discriminator: ReadonlyUint8Array };

export type CancelGoalInstructionDataArgs = {};

export function getCancelGoalInstructionDataEncoder(): FixedSizeEncoder<CancelGoalInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CANCEL_GOAL_DISCRIMINATOR }),
  );
}

export function getCancelGoalInstructionDataDecoder(): FixedSizeDecoder<CancelGoalInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCancelGoalInstructionDataCodec(): FixedSizeCodec<
  CancelGoalInstructionDataArgs,
  CancelGoalInstructionData
> {
  return combineCodec(
    getCancelGoalInstructionDataEncoder(),
    getCancelGoalInstructionDataDecoder(),
  );
}

export type CancelGoalInput<
  TAccountOwner extends string = string,
  TAccountGoalAccount extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  goalAccount: Address<TAccountGoalAccount>;
};

export function getCancelGoalInstruction<
  TAccountOwner extends string,
  TAccountGoalAccount extends string,
  TProgramAddress extends Address = typeof RIVESEEK_GOAL_VAULT_PROGRAM_ADDRESS,
>(
  input: CancelGoalInput<TAccountOwner, TAccountGoalAccount>,
  config?: { programAddress?: TProgramAddress },
): CancelGoalInstruction<TProgramAddress, TAccountOwner, TAccountGoalAccount> {
  // Program address.
  const programAddress =
    config?.programAddress ?? RIVESEEK_GOAL_VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: false },
    goalAccount: { value: input.goalAccount ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.goalAccount),
    ],
    data: getCancelGoalInstructionDataEncoder().encode({}),
    programAddress,
  } as CancelGoalInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountGoalAccount
  >);
}

export type ParsedCancelGoalInstruction<
  TProgram extends string = typeof RIVESEEK_GOAL_VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    goalAccount: TAccountMetas[1];
  };
  data: CancelGoalInstructionData;
};

export function parseCancelGoalInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCancelGoalInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { owner: getNextAccount(), goalAccount: getNextAccount() },
    data: getCancelGoalInstructionDataDecoder().decode(instruction.data),
  };
}
