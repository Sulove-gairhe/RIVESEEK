/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
  type WritableSignerAccount,
} from "@solana/kit";
import { findVaultAuthorityPda } from "../pdas";
import { RIVESEEK_GOAL_VAULT_PROGRAM_ADDRESS } from "../programs";
import {
  expectAddress,
  getAccountMetaFactory,
  type ResolvedAccount,
} from "../shared";

export const CLOSE_GOAL_DISCRIMINATOR = new Uint8Array([
  253, 46, 15, 56, 118, 220, 140, 203,
]);

export function getCloseGoalDiscriminatorBytes() {
  return fixEncoderSize(getBytesEncoder(), 8).encode(CLOSE_GOAL_DISCRIMINATOR);
}

export type CloseGoalInstruction<
  TProgram extends string = typeof RIVESEEK_GOAL_VAULT_PROGRAM_ADDRESS,
  TAccountOwner extends string | AccountMeta<string> = string,
  TAccountGoalAccount extends string | AccountMeta<string> = string,
  TAccountVaultAuthority extends string | AccountMeta<string> = string,
  TAccountVaultToken extends string | AccountMeta<string> = string,
  TAccountTokenProgram extends string | AccountMeta<string> =
    "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountOwner extends string
        ? WritableSignerAccount<TAccountOwner> &
            AccountSignerMeta<TAccountOwner>
        : TAccountOwner,
      TAccountGoalAccount extends string
        ? WritableAccount<TAccountGoalAccount>
        : TAccountGoalAccount,
      TAccountVaultAuthority extends string
        ? ReadonlyAccount<TAccountVaultAuthority>
        : TAccountVaultAuthority,
      TAccountVaultToken extends string
        ? WritableAccount<TAccountVaultToken>
        : TAccountVaultToken,
      TAccountTokenProgram extends string
        ? ReadonlyAccount<TAccountTokenProgram>
        : TAccountTokenProgram,
      ...TRemainingAccounts,
    ]
  >;

export type CloseGoalInstructionData = { discriminator: ReadonlyUint8Array };

export type CloseGoalInstructionDataArgs = {};

export function getCloseGoalInstructionDataEncoder(): FixedSizeEncoder<CloseGoalInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({ ...value, discriminator: CLOSE_GOAL_DISCRIMINATOR }),
  );
}

export function getCloseGoalInstructionDataDecoder(): FixedSizeDecoder<CloseGoalInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
  ]);
}

export function getCloseGoalInstructionDataCodec(): FixedSizeCodec<
  CloseGoalInstructionDataArgs,
  CloseGoalInstructionData
> {
  return combineCodec(
    getCloseGoalInstructionDataEncoder(),
    getCloseGoalInstructionDataDecoder(),
  );
}

export type CloseGoalAsyncInput<
  TAccountOwner extends string = string,
  TAccountGoalAccount extends string = string,
  TAccountVaultAuthority extends string = string,
  TAccountVaultToken extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  goalAccount: Address<TAccountGoalAccount>;
  vaultAuthority?: Address<TAccountVaultAuthority>;
  vaultToken: Address<TAccountVaultToken>;
  tokenProgram?: Address<TAccountTokenProgram>;
};

export async function getCloseGoalInstructionAsync<
  TAccountOwner extends string,
  TAccountGoalAccount extends string,
  TAccountVaultAuthority extends string,
  TAccountVaultToken extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof RIVESEEK_GOAL_VAULT_PROGRAM_ADDRESS,
>(
  input: CloseGoalAsyncInput<
    TAccountOwner,
    TAccountGoalAccount,
    TAccountVaultAuthority,
    TAccountVaultToken,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  CloseGoalInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountGoalAccount,
    TAccountVaultAuthority,
    TAccountVaultToken,
    TAccountTokenProgram
  >
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? RIVESEEK_GOAL_VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    goalAccount: { value: input.goalAccount ?? null, isWritable: true },
    vaultAuthority: { value: input.vaultAuthority ?? null, isWritable: false },
    vaultToken: { value: input.vaultToken ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.vaultAuthority.value) {
    accounts.vaultAuthority.value = await findVaultAuthorityPda({
      goalAccount: expectAddress(accounts.goalAccount.value),
    });
  }
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.goalAccount),
      getAccountMeta(accounts.vaultAuthority),
      getAccountMeta(accounts.vaultToken),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getCloseGoalInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseGoalInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountGoalAccount,
    TAccountVaultAuthority,
    TAccountVaultToken,
    TAccountTokenProgram
  >);
}

export type CloseGoalInput<
  TAccountOwner extends string = string,
  TAccountGoalAccount extends string = string,
  TAccountVaultAuthority extends string = string,
  TAccountVaultToken extends string = string,
  TAccountTokenProgram extends string = string,
> = {
  owner: TransactionSigner<TAccountOwner>;
  goalAccount: Address<TAccountGoalAccount>;
  vaultAuthority: Address<TAccountVaultAuthority>;
  vaultToken: Address<TAccountVaultToken>;
  tokenProgram?: Address<TAccountTokenProgram>;
};

export function getCloseGoalInstruction<
  TAccountOwner extends string,
  TAccountGoalAccount extends string,
  TAccountVaultAuthority extends string,
  TAccountVaultToken extends string,
  TAccountTokenProgram extends string,
  TProgramAddress extends Address = typeof RIVESEEK_GOAL_VAULT_PROGRAM_ADDRESS,
>(
  input: CloseGoalInput<
    TAccountOwner,
    TAccountGoalAccount,
    TAccountVaultAuthority,
    TAccountVaultToken,
    TAccountTokenProgram
  >,
  config?: { programAddress?: TProgramAddress },
): CloseGoalInstruction<
  TProgramAddress,
  TAccountOwner,
  TAccountGoalAccount,
  TAccountVaultAuthority,
  TAccountVaultToken,
  TAccountTokenProgram
> {
  // Program address.
  const programAddress =
    config?.programAddress ?? RIVESEEK_GOAL_VAULT_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    owner: { value: input.owner ?? null, isWritable: true },
    goalAccount: { value: input.goalAccount ?? null, isWritable: true },
    vaultAuthority: { value: input.vaultAuthority ?? null, isWritable: false },
    vaultToken: { value: input.vaultToken ?? null, isWritable: true },
    tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedAccount
  >;

  // Resolve default values.
  if (!accounts.tokenProgram.value) {
    accounts.tokenProgram.value =
      "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA" as Address<"TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA">;
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta(accounts.owner),
      getAccountMeta(accounts.goalAccount),
      getAccountMeta(accounts.vaultAuthority),
      getAccountMeta(accounts.vaultToken),
      getAccountMeta(accounts.tokenProgram),
    ],
    data: getCloseGoalInstructionDataEncoder().encode({}),
    programAddress,
  } as CloseGoalInstruction<
    TProgramAddress,
    TAccountOwner,
    TAccountGoalAccount,
    TAccountVaultAuthority,
    TAccountVaultToken,
    TAccountTokenProgram
  >);
}

export type ParsedCloseGoalInstruction<
  TProgram extends string = typeof RIVESEEK_GOAL_VAULT_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    owner: TAccountMetas[0];
    goalAccount: TAccountMetas[1];
    vaultAuthority: TAccountMetas[2];
    vaultToken: TAccountMetas[3];
    tokenProgram: TAccountMetas[4];
  };
  data: CloseGoalInstructionData;
};

export function parseCloseGoalInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedCloseGoalInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    // TODO: Coded error.
    throw new Error("Not enough accounts");
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      owner: getNextAccount(),
      goalAccount: getNextAccount(),
      vaultAuthority: getNextAccount(),
      vaultToken: getNextAccount(),
      tokenProgram: getNextAccount(),
    },
    data: getCloseGoalInstructionDataDecoder().decode(instruction.data),
  };
}
